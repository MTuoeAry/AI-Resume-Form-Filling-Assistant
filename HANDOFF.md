# 项目交接文档

更新时间：2026-09-07（Asia/Shanghai）

## 1. 任务目标

把本项目建设成一个泛化性较强的 Chrome/Edge 简历自动填写扩展：用户维护一份可导入、导出的标准简历文档，扩展识别不同招聘网站的字段语义、重复条目和控件类型，将标准简历可靠地映射并填入页面，同时保留人工复核和手动提交。

核心目标不是针对美的、携程、海康威视、北森或上汽通用五菱等单个网站写死逻辑，而是形成“通用扫描与语义规则为主、平台适配器为辅、AI 仅补充映射、本地确定性填充和验证”的架构。

## 2. 当前项目状态

- Git 分支：`master`
- 当前 HEAD / `origin/master`：`6726518`（`v1.0_test`）
- 工作区状态：有大量未提交修改和新增测试；尚未提交或推送。
- 扩展版本：`1.2.7`
- 标准简历 Schema：`v7`
- 字段映射缓存键：`fieldMappingCacheV13`
- 内容脚本握手版本：`2026-09-03-semantic-type-guards-v13`
- 构建方式：无构建步骤，源码目录可直接作为“已解压的扩展程序”加载。
- 自动测试：2026-09-07 本地执行 `npm test`，154/154 通过。
- 阶段判断：通用架构、Schema、映射防错、重复条目、日期和诊断能力已经完成一轮系统性改造；处于“代码回归通过、真实招聘网站重新验收待完成”的阶段，尚不应宣称所有目标网站已经适配成功。

## 3. 已完成内容

### 3.1 标准简历与数据维护

- 重复区块均允许 0 条，包括教育、工作、实习、项目、校园、证书、语言、获奖、专利、论文和自定义字段；应届生可以完全不填工作经历。
- 工作经历和实习经历在 Schema 中分离；旧数据中明显属于实习的记录会从 `workExperiences` 迁移到 `internships`。
- Schema 升级到 v7，补充身高、体重、家庭所在地、申请声明与合规、教育成绩/排名/补考重修/双学位/专升本等字段。
- “最高学历”“英语证书”“成果类别”等页面常见汇总值改为从规范数据动态推导，避免再维护一份重复的“校招补充”数据。
- 兼容旧 `campusApplication` 数据并迁移到规范字段。
- 已有 `.airesume.json` 便携文档导入、合并、完全替换和导出能力；附件和 API Key 不写入该文档。

### 3.2 字段扫描和语义映射

- 新增并扩展 `shared/field-semantics.js`，覆盖个人信息、求职偏好、技能、教育、实习、工作、项目、校园、证书、语言、奖项、专利和论文等语义。
- 同名/近义字段采用“更具体的标签优先”，避免“专业排名”被“专业”截获、“双学位”被“学历”截获。
- 项目职责、项目描述、项目成果使用不同字段：`role`、`description`、`highlights`。
- 对性别、邮箱、电话、URL、日期等强语义/强类型字段增加兼容性门禁；AI 返回不兼容路径时会被本地拒绝。
- 紧急联系人姓名/电话、护照姓名等字段与求职者本人姓名/电话明确隔离。
- 当容器上下文同时包含多个字段标签时，不再据此授权精确映射，避免整段表单文本污染单个字段判断。
- “论文是否发表”“暂无某类经历”等没有精确标准数据来源的页面状态开关会保守跳过。
- 映射优先使用本地确定性规则，AI 只处理剩余字段；所有路径必须存在于 Schema 目录中。

### 3.3 重复经历与增量填写

- 为重复区块加入更严格的区块标题、行标签、条目索引和新增按钮识别。
- 泛化支持卡片式“编辑/修改”入口，不包含海康威视域名硬编码；“添加”不会被误当成“编辑”。
- 准备重复区块时会检查页面现有条目数量、区块归属和控件增长，降低无条件新建空条目的风险。
- 页面只有“工作经历”但标准简历只有实习时，会建立临时页面投影；不会修改标准简历中经历的真实类型。
- 增量填入会先通过公司、职位、项目名、奖项名等锚点对齐页面现有条目，再把空白行分配给尚未出现的简历条目，不再简单从第 0 条开始。
- 增量模式在主循环和 `fillOne` 两层检查现有值，防止覆盖已经填写的内容。
- 单个“至今/当前”复选框按布尔值切换，能够在值为“否”时取消勾选。

### 3.4 控件类型和日期填写

- 扫描阶段能够把可访问的 `role=combobox`、`aria-haspopup=listbox/tree` 根节点识别为自定义选择器，并去重内部代理输入框。
- 下拉框、单选、多选、搜索型下拉、级联控件、普通文本、只读日期控件分别走不同填充路径。
- 日期精度由目标控件决定：年控件只填年、年月控件只填年月、日控件要求完整日期；目标控件精度优先于 AI transform。
- 支持从独立年/月/日下拉框中识别日期组成部分。
- 日期面板按年、月、日逐层选择，并排除相邻月份的同日单元格。
- 失败的普通文本写入和搜索型下拉输入会回滚原值，避免错误值残留造成“看起来填入成功”。
- `impactFactor` 只会写入紧凑数值，避免把整段论文说明写进数据库短字段。

### 3.5 诊断、缓存和运行一致性

- 侧边栏请求携带 `actionKey`、填充模式、范围和请求 ID；内容脚本返回同一执行信息。
- 侧边栏校验内容脚本版本和实际执行模式，发现旧脚本或按钮/模式不一致时明确报错。
- 调试日志记录扩展版本、内容脚本版本、操作类型、映射、原值、最终值和填充结果。
- 日志目录选择已经是 `debug-logs` 时不会再次创建嵌套目录；旧的嵌套目录仍保留，未自动删除。
- 映射缓存升级至 V13，避免复用旧版本生成的错误映射。
- 映射缓存签名包含重复条目序号并对页面已填值做稳定化处理。

### 3.6 已完成验证

- `npm test`：154 项测试全部通过。
- 覆盖 Schema 归一化、旧数据迁移、0 条重复区块、字段语义、错误映射拦截、工作/实习投影、重复条目索引、增量锚点对齐、日期精度、日期面板、自定义下拉、失败回滚、日志和版本握手。
- `node --check content.js` 和 `node --check shared/field-semantics.js` 已通过。
- `git diff --check` 未发现空白错误；仅出现 Windows 下 LF 将转换为 CRLF 的提示。

## 4. 未完成内容

1. **真实网站重新验收（最高优先级）**：重新加载扩展并刷新网页后，在美的、携程、北森/智联招聘平台、海康威视、上汽通用五菱等已反馈页面重新测试。重点检查日期、下拉框、重复项目、获奖/论文、工作-only 页面和增量模式。
2. **采集 v1.2.7 新日志**：当前最新真实日志仍是 2026-09-03 14:10 左右生成的旧版运行记录，无法证明 V13 规则在真实页面上的最终效果。
3. **端到端自动化用例**：目前以 Node 单元/片段测试为主；需要把典型 React/Vue/北森 Phoenix 日期、选择器和重复卡片抽成稳定 HTML fixture，再用 Playwright 验证完整扫描到填充链路。
4. **README 同步**：README 仍写“Schema v5”，实际已经是 v7；新增映射防错、Schema 字段和当前测试数量也未完整反映。
5. **提交与发布**：当前修改未提交、未打标签、未推送；需要先人工检查差异和真实页面验收，再提交到 GitHub。
6. **长尾页面能力**：跨域 iframe、关闭 Shadow DOM、Canvas 控件、验证码、在线裁剪和专有上传协议仍可能需要人工或专门适配。

## 5. 当前问题与阻塞点

- **缺少修复后的真实运行证据**：没有 v1.2.7/V13 生成的多站点日志，这是当前主要阻塞点。
- **需要登录态和人工页面操作**：目标招聘网站通常依赖用户登录、动态表单和真实简历状态，纯离线测试不能完全复现。
- **工作区没有会话开始前的 Git 状态快照**：当前相对 `6726518` 的差异与本次长任务内容高度一致，但仅靠 Git 无法 100% 证明所有未提交行都由本次会话产生；提交前需要用户最终浏览一次变更。
- **旧日志目录已嵌套**：代码已防止继续嵌套，但历史 `debug-logs/debug-logs/...` 没有删除，以免误删用户诊断资料。
- **README 状态过期**：不影响运行，但会误导后续维护者。
- **`content.js` 已明显膨胀**：功能可用但维护成本上升，后续可考虑把重复区块、控件运行时、日期面板拆分为独立 shared 模块。当前不要在验收前做大规模重构。

## 6. 关键实现说明

### 6.1 重要文件

- `manifest.json`：Chrome MV3 配置，当前版本 1.2.7。
- `background.js`：AI 请求代理和字段映射提示词；AI 只返回字段路径与 transform。
- `popup.js`：侧边栏操作、执行会话、版本握手、缓存清理、日志导出。
- `resume-editor.js`：独立简历配置页；统计时排除 derived 字段，避免重复计数。
- `content.js`：页面扫描、重复区块准备、增量对齐、映射归一化、控件填充、日期面板和填充后验证。
- `shared/resume-schema.js`：Schema v7、旧数据迁移、derived 字段和规范化逻辑。
- `shared/field-semantics.js`：区块/字段语义、确定性路径、类型兼容门禁、工作-only 投影。
- `shared/fill-runtime.js`：日期控件识别、目标日期精度和写入值归一化。
- `shared/site-adapters.js`：保守通用重复区块规则；美的和北森/Zhiye/iTalent 为轻量平台适配器。
- `shared/log-export.js`：诊断日志载荷、脱敏、目录和文件写入。
- `shared/content-bridge.js`：内容脚本能力和版本握手常量。
- `tests/schema-semantic-coverage.test.js`：主 Schema 字段的确定性映射与兼容性回归。

### 6.2 关键设计决策

- 标准简历是唯一事实来源；页面临时映射不能反向改变数据类型。
- 最高学历、英语证书和成果类别使用 derived 路径，避免“校招补充”与教育/奖项/论文重复存储。
- AI 映射不是最终真相：结果必须经过 Schema 路径白名单、本地语义纠正和控件类型兼容检查。
- 无法唯一判断时宁可跳过并写日志，也不进行高风险错误填充。
- 增量填入的身份由页面现有内容锚点决定，不由 DOM 顺序或缓存中的旧序号决定。
- 所有填充都以实际控件类型和目标精度为准；日期源数据精确到日不代表页面一定要填日。
- 失败填充必须验证并回滚，不能把“调用过写值函数”当作成功。
- 站点适配器只描述控件/区块能力，通用业务语义放在共享模块；不要为每个反馈网址复制整套逻辑。

### 6.3 常用命令

```powershell
npm test

Get-ChildItem -Recurse -Filter *.js |
  Where-Object { $_.FullName -notmatch '\\node_modules\\' } |
  ForEach-Object { node --check $_.FullName }

node -e "JSON.parse(require('fs').readFileSync('manifest.json','utf8')); console.log('manifest ok')"

git status --short
git diff --check
git diff --stat
```

## 7. 下一步计划

1. 在 `chrome://extensions/` 重新加载源码目录，并刷新所有目标招聘页面，确认侧边栏日志显示扩展 1.2.7 和内容脚本 `semantic-type-guards-v13`。
2. 依次执行整页、增量、选区三种模式；每个站点只检查一组明确字段，避免一次混入太多变量。
3. 优先验证五类高风险用例：性别/姓名隔离、工作-only 页面中的实习投影、项目/奖项/论文多条记录、日期年/月/日精度、增量不覆盖且不重复新增。
4. 保存每次新日志并按“扫描 → 映射 → 取值 → 填充 → 页面最终值”定位问题；不得只看状态文件名中的 success/partial。
5. 若真实页面仍失败，先补可复现 fixture 和失败测试，再修改共享规则；只有确认是平台控件差异时才增加适配器配置。
6. 更新 README 中的 Schema 版本、能力和测试说明。
7. 运行完整 CI 等价检查，审阅 `CHANGESET.md` 中的候选提交文件，再提交并推送 GitHub。

## 8. 踩过的坑

- 只根据一个宽泛词做包含匹配会造成严重串位，例如“姓名”截获“紧急联系人姓名”，“专业”截获“专业排名”。应按具体度评分并设置强约束。
- 把整个卡片/表单容器文本当字段标签会让同一区块内所有字段互相污染。长上下文只能在语义唯一时作为弱证据。
- 仅依赖 AI 修正映射不可靠；AI 返回合法路径也可能语义错误，必须本地二次门禁。
- 重复区块不能按页面顺序无条件映射第 0、1、2 条；增量时尤其需要用已填内容锚点对齐。
- “页面只有工作经历”不等于应把实习永久迁移成工作经历；只能做页面级临时投影。
- 搜索型下拉不是普通文本框。直接写入搜索词但不选择选项，会留下姓名等脏值；失败必须恢复。
- `input[type=date]`、年月选择器、独立年月日下拉和日历面板不是同一种控件；不要统一写 `YYYY-MM-DD`。
- 只统计“尝试写入”会产生假成功。成功必须以控件最终值或选中状态为准。
- 无条件点击“添加/新增”会不断制造空白经历；自动展开与自动新增必须严格区分。
- 重载扩展后，已打开页面可能仍有旧 content script；只重载扩展而不刷新页面会出现按钮模式与脚本版本不一致。
- 让用户直接选择 `debug-logs` 目录时再创建同名子目录，会形成无限嵌套；必须识别当前目录名。
- Chrome MV3 service worker 的 `importScripts` 路径必须相对扩展根目录且文件真实存在；出现加载错误时先检查路径和扩展实际加载目录，不要先猜业务代码。
- 不要删除旧日志或用户简历文件来“清理问题”；这些是复现依据和敏感用户数据。

## 9. 启动建议

新会话建议按以下顺序开始：

1. 阅读 `HANDOFF.md`、`CHANGESET.md`、`PROJECT_LESSONS.md` 和 `TASK_RETROSPECTIVE.md`。
2. 阅读 `README.md` 了解入口，但注意其中 Schema v5 已过期；以 `shared/resume-schema.js` 的 `version: 7` 为准。
3. 执行 `git status --short`、`git diff --stat`、`git log -5 --oneline --decorate`，确认没有新增的用户改动。
4. 执行 `npm test`、全部 JavaScript `node --check`、`manifest.json` JSON 解析和 `git diff --check`。
5. 查看 `E:\ai_resume_form_filling_assistant\debug-logs` 中最新日志的时间、扩展版本和内容脚本版本；旧日志仅用于回溯，不作为当前版本验收依据。
6. 真实网站测试前重新加载扩展并刷新网页；先做小范围选区测试，再做整页和增量测试。
7. 不要先重构 `content.js`；先完成真实页面验收和 Git 提交，再单独规划模块拆分。

