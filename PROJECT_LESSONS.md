# 项目可复用经验

本文件保存长期排查与实现原则，不记录一次性进度。原则描述的是维护要求，不代表所有控件已满足。当前实现见 [HANDOFF.md](HANDOFF.md)，任务和验收标准见 [ROADMAP.md](ROADMAP.md)。

## 1. 先建立可复现证据链，再修改映射

- 适用场景：用户反馈某站点“填错、没填、只填第一条或日志显示成功”。
- 错误表现或问题：只看最终截图或状态名就猜原因，扫描、映射、取值和写入阶段混在一起。
- 正确做法：按最新日志依次核对“运行版本 → 扫描字段 → 区块/条目 → 映射路径 → 原始值/转换值 → 写入结果 → 页面最终状态”。
- 执行前检查项：日志绝对路径、修改时间、扩展版本、内容脚本版本、操作模式、页面是否已刷新。
- 验证方法：每个失败字段都能指出首次偏离发生在哪一阶段；修复后生成新版本日志对照。

## 2. 字段映射必须同时使用语义、类型和基数

- 适用场景：不同网站字段命名不一致、同一区块出现大量近义字段。
- 错误表现或问题：仅凭包含匹配把姓名填进性别、专业填进专业排名、项目描述填进项目职责。
- 正确做法：以“区块语义 + 精确字段角色 + 控件类型/选项域 + 重复条目索引”共同决定路径；具体词优先于宽泛词。
- 执行前检查项：标签、id/name、placeholder、第一近邻标签、选项、sectionKey、sectionItemIndex 是否齐全。
- 验证方法：运行 `tests/schema-semantic-coverage.test.js` 和高风险冲突用例；AI 错误路径必须被本地纠正或拒绝。

## 3. 长容器文本只能作为弱证据

- 适用场景：React/Vue 表单没有标准 label，只能收集父容器文字。
- 错误表现或问题：一个卡片的 context 同时包含姓名、性别、电话等，所有控件被映射到同一字段。
- 正确做法：优先直接属性和第一近邻标签；只有长上下文指向唯一字段语义时才采用，否则交给更安全的证据或跳过。
- 执行前检查项：context 是否包含多个已知字段词；nearbyLabels 的排序是否可信。
- 验证方法：构造多标签 context，确认兼容门禁拒绝任意精确路径。

## 4. 页面控件类型先于写值策略

- 适用场景：文本框后来变成下拉框、搜索选择器、级联选择器、单选或日期面板。
- 错误表现或问题：统一调用 `input.value=`，页面看似有字但没有真实选中值，提交时为空或报错。
- 正确做法：扫描先生成统一 runtime kind；select/radio/checkbox/custom picker/date/contenteditable 分别处理。
- 执行前检查项：DOM tag、role、aria-haspopup、readonly、选项列表、日历图标、框架根节点和代理 input。
- 验证方法：读取最终 value/checked/selected 展示；触发页面校验或提交前校验但不自动提交。

## 5. 填充操作必须是可验证、可回滚的事务

- 适用场景：受控组件、搜索型下拉、框架异步重渲染。
- 错误表现或问题：写入失败却留下搜索词或临时 DOM 值，导致姓名显示在性别框等脏状态。
- 正确做法：保存原值，写入并触发事件，等待框架响应，验证最终值；失败恢复原值并记录原因。
- 执行前检查项：原元素是否仍 connected、原值/属性值、只读状态、期望验证规则。
- 验证方法：成功路径最终值匹配；失败路径原值保持不变；对应回滚测试通过。

## 6. 重复条目身份不能依赖 DOM 顺序

- 适用场景：项目、获奖、论文、工作/实习等多条经历，尤其是增量填入和选区填入。
- 错误表现或问题：每次都从第一条来源开始，覆盖已有卡片或所有行都填同一个项目。
- 正确做法：用项目名、奖项名、公司+职位、学校+专业等稳定锚点对齐；维护已占用来源索引；只给空白行分配剩余来源。
- 执行前检查项：页面已有卡片值、来源条目锚点、重复区块归属、缓存签名是否包含索引。
- 验证方法：打乱页面顺序后仍映射正确；已存在前两条时空白行应选择第三条。

## 7. 增量模式需要两层不覆盖保护

- 适用场景：用户已经手填部分字段或页面框架会重建节点。
- 错误表现或问题：扫描时认为为空，写入时节点已有值；或入口判断遗漏导致覆盖。
- 正确做法：主循环写入前刷新 DOM 引用并检查一次，底层填充函数在 `overwrite=false` 时再次检查。
- 执行前检查项：runtime.el 是否仍在文档中；自定义选择器是否已有 data-value/selected node。
- 验证方法：预填字段保持不变，空字段可填；框架重渲染后的同名新节点也不被覆盖。

## 8. 数据事实和页面承载方式必须分离

- 适用场景：求职者只有实习，但网站只有“工作经历”区块。
- 错误表现或问题：为适配页面把实习永久改成工作经历，污染标准简历。
- 正确做法：标准简历保持真实类型；构造按时间排序的页面级投影，并在映射阶段把每个页面行指回原来源。
- 执行前检查项：页面是否确实没有实习区块；工作和实习来源数量、时间和锚点。
- 验证方法：页面填入成功后重新读取本地 profile，`internships`/`workExperiences` 类型未变化。

## 9. 日期输出精度由目标控件决定

- 适用场景：同一标准日期需要填入年份、年月、完整日期或分离下拉框。
- 错误表现或问题：年/月控件收到年月日，日控件只收到年月，或 AI transform 覆盖真实控件形状。
- 正确做法：标准数据保留最高可知精度；运行时识别 year/month/day/组件部分，目标控件精度优先。
- 执行前检查项：input type、format 属性、placeholder、面板结构、独立 select 的选项范围、源日期是否包含所需部分。
- 验证方法：对同一 `YYYY-MM-DD` 分别断言年、月、日控件结果；日控件缺 day 时应跳过而非猜 1 日。

## 10. 自动新增和自动展开必须分离

- 适用场景：页面有“展开更多”“编辑”“添加经历”等相似按钮。
- 错误表现或问题：为了扫描字段自动点击“新增”，产生多余空卡片；或把“添加”当“编辑”。
- 正确做法：展开只允许明确 expand/collapsed 语义；新增只在对应重复区块缺少条目且能确认控件增长时执行；编辑入口单独识别。
- 执行前检查项：按钮文本、type、aria-expanded、区块标题、行标签、当前条目数和所需条目数。
- 验证方法：深度扫描测试保证不点击新增；卡片测试保证 edit 可识别、add 不等于 edit。

## 11. 派生字段用于消除重复存储

- 适用场景：最高学历、英语等级、奖项/论文/专利类别等汇总字段。
- 错误表现或问题：同时维护教育记录和“校招补充”，值冲突且用户不知道该填哪一份。
- 正确做法：事实字段只存一次；汇总值通过 `derived.*` 读取；旧结构仅作为迁移输入。
- 执行前检查项：新字段能否从既有结构稳定推导；排序规则、空值和旧数据兼容。
- 验证方法：调换教育列表顺序仍选出同一最高学历；显式空列表不会被旧汇总文本重新生成。

## 12. 通用规则与站点适配器要有边界

- 适用场景：连续收到不同招聘网站失败反馈。
- 错误表现或问题：每个网址添加一套硬编码，前一个站点修好后下一个站点继续失败。
- 正确做法：字段语义、重复索引、日期和控件生命周期放共享模块；适配器只描述平台选择器、区块根和选项节点。
- 执行前检查项：失败是否能用通用 DOM/ARIA 模式解释；是否真的是平台私有结构。
- 验证方法：加入未知域名 fixture；测试源码不出现仅为案例页面添加的 hostname 分支。

## 13. 版本、缓存和日志必须能证明运行的是新代码

- 适用场景：扩展重载后结果与修改不一致，或日志看起来仍执行旧模式。
- 错误表现或问题：旧 content script、旧映射缓存和新侧边栏混用，误以为修复无效。
- 正确做法：请求/响应携带 action、mode、scope、requestId 和内容脚本版本；破坏性映射调整时升级缓存键。
- 执行前检查项：manifest 版本、content bridge 版本、缓存键、页面是否刷新。
- 验证方法：新日志中的请求与响应版本完全一致；旧脚本明确报错而不是静默继续。

## 14. 声称“成功”前必须验证外部可见结果

- 适用场景：浏览器自动化、文件导出、表单填充等有外部状态的操作。
- 错误表现或问题：函数返回、日志写出或文件名包含 success，但页面/文件实际没有目标结果。
- 正确做法：定义可观察后置条件并读取验证；无法验证时使用“待确认/部分完成”。
- 执行前检查项：成功判据、可读取的最终状态、失败和 no-change 的区别。
- 验证方法：页面最终控件值、文件内容、条目数量或测试断言与预期一致。

## 15. 存储结构迁移必须为代码回退保留数据通道

- 适用场景：把浏览器扩展从单记录存储升级为多模板、分库或新 Schema。
- 错误表现或问题：迁移成功后立即删除旧键；一旦用户回退扩展版本，旧代码读不到新结构，表现为简历丢失。
- 正确做法：迁移新结构的同时持续镜像当前活动数据到旧键；新结构损坏时优先从兼容快照恢复，只清理不再被任何可回退版本使用的更早期键。
- 执行前检查项：旧版读取顺序、新旧存储键、迁移失败路径、回退版本能否读取、重复数据的空间成本。
- 验证方法：覆盖“旧数据升级”“活动模板切换后旧键同步”“新结构损坏恢复”三类测试，并实际用旧版代码读取一次兼容快照。

## 16. 实时输入值不能冒充默认值

- 适用场景：填入后看似成功，再次点击字段却清空。
- 错误表现或问题：同时写 `.value` 与 HTML `value` 属性，导致 defaultValue 变为简历内容；旧网站会把两者相等的值当提示文字清掉。
- 正确做法：按原生值 setter 和网站事件协议修改实时值，保留初始默认值；只有已证实的控件协议才允许修改默认属性。
- 执行前检查项：聚焦/失焦处理、defaultValue、受控组件状态、失败恢复路径。
- 验证方法：填写后再次聚焦、失焦和重新渲染；隔离样例允许时再保存/重开，确认值不丢失。仅一次读取 `.value` 不足以验证完整生命周期。

## 17. 通用性需要独立样本，覆盖率需要独立分母

- 适用场景：单站修复、本地样例通过或日志显示“全部成功”。
- 错误表现或问题：所有样例都按当前规则设计；漏扫描字段没进入统计，覆盖率虚高。
- 正确做法：保留未用于规则开发的页面/布局作独立验收；人工列出本轮目标字段和条件，分别统计发现、映射、写入、最终验证及缺少资料。
- 执行前检查项：样本是否独立、人工字段清单、可操作范围、原有值和简历事实是否明确。
- 验证方法：参考 ROADMAP 的评测口径；保留原值不一律计作正确填写，未测试记为未测试，不把 jsdom 通过当作真实扩展或站点通过。

## 18. 交接分开记录当前状态、历史证据与未来要求

- 适用场景：多个会话连续修改，文档同时出现旧版本和新版本。
- 错误表现或问题：历史“当前状态”继续被引用；新代码沿用旧浏览器验收结果。
- 正确做法：HANDOFF 保持单一当前基线，历史记录注明阶段，ROADMAP 列待办；每条验证绑定运行版本、环境和范围。
- 验证方法：接手者能区分未提交代码、已通过自动测试、已实测浏览器和已实测网站；无需从多段互相矛盾的“最新状态”猜测。
