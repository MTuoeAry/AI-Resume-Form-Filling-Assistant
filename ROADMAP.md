# 跨网站简历填写：后续优化路线

更新：2026-09-09。当前实现和验证基线见 [HANDOFF.md](HANDOFF.md)，开发步骤见 [CONTRIBUTING.md](CONTRIBUTING.md)。本文任务和验收门槛是后续工作要求，不是已完成的功能声明。

## 1. 最终应该做到什么

2026-09-08 产品方向补充：新增“选填与复制”作为用户主动控制的入口。用户负责新增/展开/保存，指定网页编辑区并选择任意来源记录；侧边栏复制始终独立可用。此模式已实现，详见 HANDOFF 和 README。下面的自动推进任务属于保留的整页模式，不应强制成为选填或复制的前置条件。

用户选好标准简历，扩展理解不同网站的表单结构，将相应经历填入正确位置；保留已有内容，对不能确定的项目说明原因，用户复核后提交。更换网站不应通常意味着新增一套业务映射规则。

一项可解释的填写决策至少包含：

```text
页面区块：教育经历（依据：邻近标题、ARIA、容器标识）
页面条目：与简历中某条教育经历对应（依据：学校/专业/时间锚点）
字段角色：结束时间（依据：标签或同一时间组的结构）
来源事实：educations.<已对齐索引>.endDate
控件策略：年月选择器，点击年、月并验证选中值
最终结果：已验证 / 保留已有 / 缺少资料 / 不能确定 / 执行失败
```

这是目标决策模型；当前日志和运行时尚未完整覆盖其中所有证据与后置状态。

## 2. 对当前问题的判断

总体方向无需推倒重来，问题在于结构识别、条目身份和控件执行尚未形成全链路的可靠约束。修改提示词或增加同义词只能修复其中一层。

| 问题层 | 当前代码/案例证据 | 后续方向 |
| --- | --- | --- |
| 没有扫描到 | 老页面用 div 模拟日期/选择框，新增结构模块覆盖部分模式 | 单独测字段发现率，补通用控件描述能力 |
| 区块或字段误判 | `educationInfo` 提供线索；`workInfo` 可与“实习经历”标题冲突；未知结构仍有旧文本回退 | 记录证据来源、冲突和边界，禁止把大段邻近正文当强证据 |
| 对错经历 | 重复容器和内容锚点已存在，但索引并非稳定身份 | 覆盖同名、空白、排序变化、复用编辑器，显式拒绝歧义 |
| 事实/角色混淆 | 学历与学位已拆分；条目级最高学历开关仍跳过 | 先定义事实和派生规则，再扩展字段，不能猜值 |
| 写入后失效 | v15 修复同时写入 defaultValue 导致聚焦清空的问题 | 验证完整事件生命周期和网站内部状态，不只看文本 |
| 验收不足 | Node 回归、v16 握手、逐条经历样本和 unpacked 扩展 E2E 已接入 | 真实招聘网站填写未测；中国移动校招页仅确认登录墙；portal 弹层和保存/重开仍待测 |

## 3. P0：先保证能定位错误、不会错填

2026-09-09 映射专项执行细则见 [字段映射可靠性实施规范](MAPPING_RELIABILITY_IMPLEMENTATION.md)。概念词典、映射校验、语言考试字段和概念记忆已有实现；完整人工纠错、证据冲突处理及独立布局验收仍需逐项对照规范，不将 A–E 视为全部完成。T 矩阵的此前执行记录与真实站点边界见该文档第 14 节。

### P0-1 建立跨布局基准和当前版本实测

- **状态**：已完成首轮冻结样本、v16 浏览器冒烟、逐条经历 E2E 和 unpacked 扩展链路。2026-09-07 访问中国移动校招简历页，因短信/图形验证码登录未能进入表单，记为未测，不能记为通过。
- **入口**：`tests/structural-integration.test.js`、`tests/helpers/dom-extension.js`、`tests/browser/`、诊断日志。
- **工作**：从反馈页面提取脱敏 DOM 和事件行为，保留层级、选项编码、动态变化；用虚构简历标注应填路径及预期状态。中国移动作为一个回归案例，登录后优先测专业论著、项目经历、校内职务的逐条保存/新增；另覆盖原生表单、ARIA 组件、异步搜索/弹层、重复卡片等结构。公开编辑页标签见 [HANDOFF.md](HANDOFF.md)，登录前不要按主机名补按钮规则。
- **验收**：当前握手版本下重跑浏览器冒烟；在真实扩展中验证侧边栏、消息代理和内容脚本；建立至少 3 类布局的可复现样本，并预留至少 2 个未用于制定规则的独立页面/布局作验收集。这些数量是首轮目标，尚未完成。
- **交付**：样本、预期字段清单、运行版本、实际结果及未通过项。缺少登录或访问条件时记录“未测”，继续离线复现工作，不能记为通过。

### P0-2 让结构证据与候选映射形成一致约束

- **状态**：随机 ID 不再因包含 education 而锁定；标题优先于标识；混合弱证据不锁定；同组日期获得 start/end 角色；缓存命中走同一套 `normalizeMappings`。弱结构页面和同义区块仍需更多真实样本。
- **入口**：`shared/page-structure.js` 的 `findSection`/`itemRoot`，`shared/field-semantics.js`，`content.js` 的扫描、候选生成、归一化与缓存读取。
- **工作**：明确区块根、条目根、字段行、角色证据及冲突处理；标题/关联标签/选项域与 ID 冲突时保留原因。候选生成、AI 返回、本地规则、缓存命中使用一致的兼容性约束。未知或歧义不能直接变成高可信锁定。
- **验收**：覆盖无 ID、随机 ID、误导性 ID、嵌套标题、多个同义区块、相邻正文干扰、同组开始/结束日期。跨区块、跨条目、错误类型路径在所有来源中均被拒绝；无法确定时不写入并给出原因。
- **约束**：不要把 `educationInfo` 或某个域名注册成唯一正确答案；日期顺序推断应受同一字段组边界约束。

### P0-3 建立稳定的重复条目身份

- **状态**：主锚点精确匹配加日期加分，并列最高分跳过；覆盖模式不改空白卡片页序。整页填写已改为逐条协调：当前记录被页面接受后再新增或复用编辑器。同名无日期、保存后重排和超时对账已有虚构样本；真实站点仍待补。
- **入口**：`shared/repeat-alignment.js`、`content.js` 的重复区块准备与编辑器发现；`shared/page-structure.js` 的 `itemRoot`。
- **工作**：区分页面位置、简历记录身份和当前编辑器；组合学校/公司、专业/职位、日期等锚点，对冲突保留歧义。新增条目与展开已有条目分开，节点重建后重新定位。
- **验收**：来源倒序、同名不同日期、空白条目、部分已有内容、删除后重排、两个同义区块、单编辑器切换多卡片。二次增量不覆盖已有值、不重复新增；一条经历不能分散到多张卡片。不能唯一对齐时解释并跳过。

### P0-4 控件写入必须在交互后仍有效

- **状态**：v15 live value、原生 date 不补 1 日、年月不补 1 月、contenteditable 校验回滚已有回归。异步搜索、portal 和保存/重开仍待验证。
- **入口**：`content.js` 的 `fillOne`、`setValueWithEvents`、选择器/日期执行与恢复函数，`shared/fill-runtime.js`。
- **工作**：定义各控件读取、操作、等待完成、验证、失败恢复的责任；检查显示值与选中编码一致。恢复通过网站原有事件/选项完成，不能恢复内部状态时报告需要人工检查。
- **验收**：聚焦/失焦不清空；搜索无结果不残留文本；异步返回不选择旧候选；受控组件重渲染后仍正确；日期保持目标精度；失败不污染其他字段。在支持保存/重开的隔离样例中验证保存后的值。
- **约束**：不得改写展示 span 或隐藏属性冒充选中成功，不得补造日期或简历事实。

## 4. P1：扩展通用能力和可解释性

| 任务 | 入口与范围 | 完成判据 |
| --- | --- | --- |
| P1-1 动态控件与重新扫描 | `content.js`、`page-structure.js`；portal 弹层归属、异步搜索、级联依赖、节点替换 | 弹层归属发起控件；新增字段重扫但不重复执行；等待/重试有上限；事件样例与真实浏览器均验证 |
| P1-2 来源缺失与派生策略 | `resume-schema.js`、`field-semantics.js`、`resume-prompts.js` | 分清未填写、明确否、不可派生；定义最高学历/最高学位/主学习经历的在读、并列、多记录规则；无充分事实仍留空，旧资料往返不丢失 |
| P1-3 诊断与人工纠正 | `diagnostics.js`、`content.js`、`popup.js` | 展示区块/条目/路径/证据/冲突/最终状态；缺资料与无映射分别说明；人工纠正限制作用范围，不污染其他网站或模板 |
| P1-4 缓存可靠性 | 缓存签名、映射读取、侧边栏清理入口 | 模板切换、条目重排、结构变化不复用失效映射；命中也走本地门禁；日志解释命中/失效原因 |
| P1-5 自动化扩展验收 | `tests/e2e/`、`tests/browser/` 与 CI | unpacked Chrome job 已覆盖注入、storage、`startFill`、v16 逐条教育与英文 holdout；仍需动态事件、模型失败和模板切换 |

## 5. P2：基于证据拆分代码与补充平台能力

`content.js` 同时负责扫描、计划和执行。本轮已抽出 `shared/repeat-alignment.js`（条目对齐）和 `shared/repeat-flow.js`（逐条推进），并把日期解析并入 `shared/fill-runtime.js`；生产入口仍是 `content.js`。测试入口稳定为 `tests/helpers/dom-extension.js` 的 `window.testApi`，映射、对齐、日期派生和部分填写守卫不再切片源码。缓存签名、深扫描、卡片编辑器和自定义选择器仍有切片测试，后续再抽映射计划与控件执行模块。

只有通用结构无法描述的平台私有交互，才放入 `shared/site-adapters.js` 或后续适配器；适配器提供能力/结构描述，不复制简历业务规则。每个适配器应有平台样例和未知域名反例。跨域 iframe、封闭 shadow root、附件裁剪/私有上传协议另做能力调查，当前不承诺通用支持。

多语言、性能优化和长期站点监测在覆盖率基线建立后安排，不能挤占错填、串条目和状态失效问题的优先级。

## 6. 统一评测口径

先人工标注页面全部目标字段，包含未扫描到、未展开和条件出现的字段，说明本轮场景与可操作范围。以下指标分别报告，不能用“已扫描字段数”代替页面总字段数：

| 指标 | 口径 |
| --- | --- |
| 字段发现率 | 扫描正确发现的目标字段 / 人工标注的本轮可操作目标字段 |
| 映射准确率 | 路径、条目、角色均正确的非空映射 / 所有非空映射 |
| 有资料字段完成率 | 最终值经验证且符合预期的目标字段 / 本轮可操作且简历有足够事实的目标字段 |
| 写入成功率 | 实际尝试写入且通过后置验证的字段 / 实际尝试写入字段 |
| 非预期改动 | 错误覆盖已有值、错误新增/删除条目、串条目数量；基准集要求为 0 |
| 二次增量稳定性 | 再执行后的字段值与条目数量变化；保留已有内容，仅处理仍需填写且能确定的项目 |
| 原因覆盖 | 未完成项归类为漏扫描、未展开/条件未满足、结构/映射歧义、缺资料、不支持、执行/验证失败 |

现有统计不包含所有上述指标，需要先在评测记录中人工补齐，再逐步进入诊断。报告区分“本次填入正确”“原有正确值保留”“原有值与预期不同但按增量规则保留”；不能把所有 preserved 都算作填写成功。分母为 0 的指标记为不适用。

每个页面建议使用以下记录模板，不填未经实测的数字：

```text
案例 ID / 页面类型 / DOM 与事件样例位置：
日期 / 扩展版本 / 内容脚本版本 / 缓存 / 提交或工作区说明：
简历样例 / 模板 / 模式 / 范围 / 模型状态：
开发回归样本或独立验收样本：
人工目标字段数 / 可操作数 / 有资料数 / 发现数：
正确映射数 / 非空映射数 / 实际写入数 / 最终验证通过数：
保留原值及差异 / 非预期覆盖 / 条目数量前后变化：
失败字段 → 首次出错阶段 → 证据 → 关联用例/任务：
二次增量 / 聚焦失焦 / 重开编辑器结果：
未测试部分及原因：
```

## 7. 后续开发者的执行顺序

1. 阅读交接，核对未提交变更和运行版本，运行现有测试。
2. 选择可复现失败，定位最早偏离发生在扫描、归属、映射、取值、执行还是验证。
3. 补真实结构/事件反例；另选不同布局防止只匹配样例。
4. 修复最早出错的一层，同步检查本地、AI、缓存入口约束。
5. 跑相关测试与全量回归；交互改动增加浏览器后置状态验证。
6. 更新基准记录和任务状态，分别列出代码实现、自动测试、浏览器实测、真实站点实测的证据。

下一轮首先交付 **P0-1 的版本一致性复验与基准清单**，再按失败分布处理 P0-2 至 P0-4；不要无限扩充字段词表，也不要把本地测试通过写成全站适配完成。
