# 本次长任务纠错复盘

本文件只分析本次协作为什么被纠正以及怎样避免再次发生，不记录项目当前进度。错误归因严格使用用户指定的分类。

| 被修改或纠正的内容 | 原先的问题 | 错误归因 | 下次应增加的开局指令 | 可执行的避免措施 |
| --- | --- | --- | --- | --- |
| 标准简历字段覆盖 | 初始 Schema 字段过少，实际网申仍需大量手填。 | 对需求理解错误；修改范围不完整 | “先对照至少三个真实网申表单盘点字段，再设计 Schema。” | 建立页面字段—Schema 差异表；新增字段后跑 Schema 目录覆盖测试。 |
| 简历数据维护入口 | 默认把复杂数据维护局限在侧边栏/配置页，没有先考虑换电脑和文件化维护。 | 对需求理解错误；未验证就作出假设 | “标准简历必须支持本地文档导入导出和跨设备迁移。” | 在设计存储前确认便携格式、合并/替换语义和敏感数据边界。 |
| 工作经历最少一条 | 应届生没有工作经历，却把列表最小数量固定为 1。 | 判断逻辑错误；忽略了项目约束 | “所有经历类区块默认允许 0 条，应届生无工作经历是正常状态。” | 对每个 list section 统一断言 `minItems=0`，增加零条回归测试。 |
| 实习映射为工作经历 | 直接把实习条目放进 `workExperiences`，破坏简历事实类型。 | 对需求理解错误；判断逻辑错误 | “实习和正式工作必须在数据层分离，页面兼容不得改写事实类型。” | 增加旧数据迁移、路径隔离和 work-only 页面临时投影测试。 |
| 其他经历也应可为空 | 只修了工作经历，没有同步检查教育、项目、奖项、论文等列表的空状态。 | 修改范围不完整 | “修复一个列表基数问题后，枚举所有同构列表做一致性审计。” | 使用 Schema 遍历测试检查所有 list section，而非逐个手改。 |
| 项目经历映射 | 项目多条记录和字段角色映射不稳定。 | 判断逻辑错误；修改范围不完整 | “重复项目必须同时识别区块、条目序号和字段角色。” | 对项目名、职责、描述、成果分别建立确定性映射和序号测试。 |
| 获奖经历映射 | 奖项没有稳定映射或多条都指向首条。 | 判断逻辑错误；未验证就作出假设 | “获奖记录按条目索引映射，并用奖项名作为增量锚点。” | 增加多奖项扫描、索引、日期和新增条目测试。 |
| 论文经历映射 | 论文标题、期刊、作者、影响因子和详情没有完整区分。 | 修改范围不完整；判断逻辑错误 | “论文字段必须结构化拆分，短数值字段不得承载长文本。” | 对 publication 全字段建立语义规则和类型/长度防护。 |
| 只围绕美的页面优化 | 早期修改过度围绕单个示例站点，用户明确要求泛化。 | 对需求理解错误；忽略了项目约束 | “示例网站只用于提取通用模式，禁止把 URL 当作业务规则。” | 共性放 shared 模块；站点适配器只保存选择器/能力；新增无域名回归测试。 |
| AI JSON 解析失败 | 默认假设模型总返回严格 JSON，代码块、尾随逗号或附加说明会失败。 | 未验证就作出假设；修改范围不完整 | “模型输出必须按不可信文本解析，并保留本地映射兜底。” | 覆盖 fenced JSON、尾随逗号、平衡 JSON 截取和直接数组测试。 |
| 选取填入总是第一条 | 框选并未解决重复条目的身份问题，仍按第一个简历条目填。 | 判断逻辑错误 | “选区只限定页面范围，条目身份仍需用页面内容和序号对齐。” | 用锚点匹配现有卡片；空白卡片分配尚未出现的来源条目。 |
| 日志显示成功但页面无结果 | 把流程完成或尝试写入误报为填充成功。 | 判断逻辑错误；未验证就作出假设 | “成功必须由页面控件最终状态验证，不接受仅凭函数无异常。” | 写入后读取最终值；0 个成功写入时输出 failed/no_changes；日志记录最终值。 |
| 日期控件被当文本框 | 获奖时间、实习时间等实际是日历/选择器，却走普通文本写值。 | 对需求理解错误；未验证就作出假设 | “扫描后先判定控件类型和日期精度，再决定填充策略。” | 识别 combobox、只读输入、日历图标和面板；按类型路由。 |
| 扩展加载 `importScripts` 错误 | service worker 引用的脚本路径/加载目录曾出错，扩展无法启动。 | 工具或命令使用错误；已有信息未被正确读取 | “修改 MV3 worker 前核对 manifest、实际文件路径和扩展加载根目录。” | 语法检查后在 `chrome://extensions` 查看 service worker 错误；路径使用扩展根相对路径。 |
| 携程时间仍填不上 | 第一次日期优化只覆盖某类组件，没有覆盖可访问自定义选择器和不同精度。 | 修改范围不完整；未验证就作出假设 | “至少用两种框架日期控件验证，不把单站成功当通用完成。” | 抽取年/月/日、年月面板、日历日面板 fixture，逐层验证。 |
| 单个项目成果文字过窄 | 只罗列指标，没有体现端到端工作量、系统性和落地价值。 | 对需求理解错误 | “项目亮点要覆盖问题规模、技术闭环、量化结果和部署落地。” | 写作前提取范围—方法—结果—影响四类证据，避免只拼指标。 |
| 三个项目亮点整体偏窄 | 修改了一处文案但没有一致性审阅其余项目。 | 修改范围不完整 | “优化一条项目文案后，横向审阅全部项目的表达层级和工作量。” | 建立统一项目亮点模板并逐项目检查是否包含规模、难点、结果、落地。 |
| 把“项目亮点”理解成其他字段 | 用户指的是截图中的具体“项目亮点”，回答却偏向另一个成果字段。 | 已有信息未被正确读取；对需求理解错误 | “先引用截图字段名复述修改目标，再开始改写。” | 读取截图标签和上下文；输出前核对目标字段名称。 |
| 代表性成绩缺少政府项目层级 | 文案只堆论文和模型指标，没有体现政府项目、工程责任和社会治理价值。 | 已有信息未被正确读取；对需求理解错误 | “保留可验证事实，同时体现政府项目场景、系统规模和落地影响。” | 从材料中提取项目委托方/应用场景；不虚构级别、合同额或政策影响。 |
| 获奖日期只能填部分且总新建一条 | 重复区块计数、日期填写和新增按钮控制相互独立，导致未填完又新增。 | 判断逻辑错误；修改范围不完整 | “重复区块准备、条目映射和日期填充必须作为同一条链路验证。” | 每次新增后确认控件增长；用现有条目数和来源数计算缺口；整链路测试。 |
| 增量模式覆盖已有内容 | 只在部分入口判断空值，框架重渲染后引用失效或第二层仍可写入。 | 判断逻辑错误；修改范围不完整 | “增量模式必须在扫描后和实际写入前双重检查当前 DOM 值。” | 刷新 runtime 元素；主循环跳过；`fillOne(overwrite=false)` 再防守。 |
| 日精度被降为月 | 把源数据格式当目标格式，未识别目标控件支持到日。 | 判断逻辑错误 | “源日期保留最高精度，最终输出由目标控件精度决定。” | 识别 target precision；日控件要求 day，月控件截断，年控件取 year。 |
| `impactFactor` 数据截断 | 将论文详情或长字符串映射/写入数据库短字段。 | 判断逻辑错误；未验证就作出假设 | “数值短字段必须有语义隔离和写入前格式验证。” | 影响因子路径强匹配；提取紧凑数值；无合法数值时跳过。 |
| 项目职责映射成项目描述 | 字段规则使用宽泛“项目”上下文，未区分 role 与 description。 | 判断逻辑错误 | “项目职责、描述、成果必须分别映射，不允许互相兜底。” | 为三者定义互斥字段词；加入回归断言。 |
| 页面没有实习区块、只有工作区块 | 强制实习只能填实习区块，导致北森类页面无法应用。 | 对需求理解错误；修改范围不完整 | “允许页面能力与数据事实不同，但只能做临时投影。” | 检测 work-only 页面；按时间组合工作/实习来源；保留原 Schema 类型。 |
| 海康威视页面再次暴露泛化不足 | 针对前一个站点修复后没有验证卡片式编辑页面。 | 修改范围不完整；未验证就作出假设 | “每次站点反馈先抽象页面模式，再加入一个未知域名反例测试。” | 实现通用 dormant editor 发现；测试源码不得包含海康域名。 |
| 增量仍从第一条开始 | 仅按 DOM 序号或缓存索引，没有识别页面已有条目对应哪个来源。 | 判断逻辑错误 | “增量条目必须基于稳定内容锚点对齐，DOM 顺序仅作最后兜底。” | 使用公司/职位/项目名/奖项名等锚点，跟踪已占用来源索引。 |
| 年/月控件仍收到年月日 | 日期 transform 由 AI 或源数据决定，覆盖了目标控件真实形状。 | 判断逻辑错误；忽略了项目约束 | “目标控件形状优先级高于 AI transform。” | 填充前推断 date component；强制提取 year/month/day 对应值。 |
| 上汽通用五菱字段缺失、下拉改造后失败 | 只识别字段名称，没有先识别控件类别和选项域；Schema 也未覆盖部分校招字段。 | 修改范围不完整；对需求理解错误 | “扫描产物必须同时包含语义、控件类型、选项、区块和条目序号。” | 构造统一 runtime；选择器必须匹配真实 option；补充字段后跑全 Schema 覆盖。 |
| “校园招聘补充”重复且含义不清 | 新增一组临时字段，和教育、奖项、论文重复；最高学历字段来源不明确。 | 对需求理解错误；判断逻辑错误 | “新增字段前先判断是事实字段、汇总字段还是可推导字段。” | 事实只存一次；最高学历从 education 推导；旧临时结构只做迁移输入。 |
| 选取填入工作经历失败 | 没有第一时间把最新日志中的扫描、映射、取值、写入逐段关联。 | 已有信息未被正确读取；未验证就作出假设 | “用户说‘看最新日志’时先按时间定位文件，再做逐阶段证据链。” | 输出最新文件时间和路径；检查请求模式、脚本版本、字段索引和最终写入。 |
| 日志目录位置纠正 | 最初按错误目录查找日志，用户指出实际在 `E:\ai_resume_form_filling_assistant`。 | 已有信息未被正确读取；工具或命令使用错误 | “先从用户给定项目根递归定位日志，不沿用旧路径假设。” | `Get-ChildItem -Recurse -Filter *.json`，按 LastWriteTime 排序并回报实际路径。 |
| 姓名映射到性别 | 允许 AI 合法路径直接通过，且搜索型性别下拉失败后可能残留姓名搜索词。 | 判断逻辑错误；修改范围不完整；未验证就作出假设 | “高风险枚举字段必须有强语义约束，失败写入必须回滚。” | 性别标签/男女选项强制 `personal.gender`；类型兼容门禁；自定义下拉失败恢复原值。 |
| 要求举一反三后才做全局自查 | 之前多次按单个 bug 修补，没有系统枚举同构字段、控件和流程风险。 | 修改范围不完整；对需求理解错误 | “每修复一个案例，同时执行同类字段、同类控件、同类列表的横向审计。” | 维护 Schema 全字段语义覆盖测试；日志批量扫描可疑映射；加入失败回滚与上下文污染测试。 |

本次共整理 34 个被纠正或要求返工的问题。
