# 开发与贡献指南

项目目标是在不同招聘网站可靠复用标准简历，同时保留用户对目标区域、来源记录和最终提交的控制。开始开发前阅读 [README](README.md)、[HANDOFF](HANDOFF.md) 和 [ROADMAP](ROADMAP.md)。映射改进还需阅读 [实施规范](MAPPING_RELIABILITY_IMPLEMENTATION.md)。

## 环境与运行

- 使用 Node.js 20 或更新版本、npm，以及支持侧边栏的 Chrome / Edge。
- 克隆仓库后运行 `npm ci` 安装锁定的开发依赖。
- 在扩展管理页开启开发者模式，加载包含 `manifest.json` 的根目录。
- 无打包步骤。修改源码后重新加载扩展；目标网页需刷新才能加载新的内容脚本，刷新前处理未保存内容。
- 修改内容脚本协议或行为时检查 `shared/content-bridge.js` 的握手版本，避免新侧边栏调用旧脚本。

正常使用扩展无需 npm；以上依赖只服务于开发和测试。

## 修改前先确认工作区

运行 `git status --short`、`git diff --stat`，分清已有修改和本次修改。不要覆盖其他人的未提交代码，也不要清理未知来源的文件。小范围功能改动优先保持现有模块边界。

本仓库核心是标准简历填写。早期问卷模板模块不是默认改动入口，不要因目录中存在旧实现就另建一套业务映射链路。

## 验证步骤

### 单元与 DOM 集成

```sh
npm test
```

测试覆盖数据规范化、映射约束、字段识别、重复经历、日期与控件执行。DOM 集成使用 `tests/helpers/dom-extension.js` 加载生产脚本；它替代布局几何、部分等待和浏览器消息，不能证明真实网页时序正确。

### Chrome 扩展 E2E

```sh
npm run test:e2e
```

需要本机可用的 Chrome。路径未被自动找到时，设置 `CHROME_PATH`。Windows PowerShell 示例：

```powershell
$env:CHROME_PATH = 'C:\Program Files\Google\Chrome\Application\chrome.exe'
$env:PLAYWRIGHT_HEADLESS = '1'
npm run test:e2e
```

请填写自己机器上实际存在的路径。测试以独立浏览器环境加载 unpacked 扩展，覆盖后台、存储、动态注入、消息和虚构表单。当前侧边栏 UI 测试使用独立 `popup.html`；不能将它等同于原生 Side Panel 容器或真实招聘站点验收。

### 手动冒烟样例

```sh
node tests/browser/serve.cjs
```

启动后可访问：

- `http://127.0.0.1:8765/tests/browser/structural-smoke.html`
- `http://127.0.0.1:8765/tests/browser/repeat-flow-smoke.html`
- `http://127.0.0.1:8765/tests/browser/record-selection-smoke.html`

使用虚构简历，检查最终 DOM、已有值保留、记录对应关系和非预期点击，结束后停止服务。真实站点只能在获得访问条件和用户授权的范围内验证；未测就记录未测。

### 提交前

```sh
git diff --check
git diff --cached --stat
git diff --cached
```

按改动范围运行相关检查；涉及数据、映射或执行链路时运行完整 Node 回归，涉及扩展加载和交互时运行 E2E。纯文档修改检查链接、命令和功能描述即可，不需要为文字改动新增测试。

## 关键实现约束

1. **业务语义共用规则。** 区分标签、当前值和选项；不能仅凭“能力”“名称”等泛词选来源。适配器描述网站控件，不复制一套域名专用简历词典。
2. **来源记录必须明确。** 字段角色与来源身份分别解析，不能把同一条来源默认铺满多条目标；用户选填必须遵守选区边界。
3. **模型结果仍需校验。** 本地规则、AI、缓存和人工概念记忆应经过一致的约束，不能因缓存命中或 AI 返回成功就绕过检查。
4. **成功必须验证。** 搜索词出现在输入框中不等于选中了选项；不要直接改展示 span 冒充控件成功。日期不得补造来源不存在的月或日。
5. **操作有界且可取消。** 不恢复无限扫描或超时重复点击，不自动提交申请。选填与复制不依赖自动新增和保存。
6. **数据升级无损。** 修改 Schema 时检查旧模板、单份文档、全部模板备份、原始文本和附件边界；迁移保持幂等，新增字段不得覆盖用户已填事实。
7. **加载入口完整。** 新增共享模块时同步核对 HTML、动态注入、测试 helper 和 E2E 脚本清单。

## 报告问题

请提供以下信息，便于重现：

- 招聘页面类型、浏览器与插件运行版本。
- 使用整页覆盖、增量、选区还是指定记录填入。
- 字段的标签、可选项、预期来源和实际结果。
- 脱敏截图、诊断日志，或能复现问题的最小 HTML/事件样例。
- 是否涉及未加载选项、保存后重建、重复记录或旧缓存。

不要把真实姓名、联系方式、证件号、完整简历、邀请链接令牌、API Key 或浏览器个人资料提交到公开仓库。测试使用虚构资料；日志导出虽有脱敏，也应在分享前检查。

## 文档和提交说明

README 面向使用者，描述当前可用行为；HANDOFF 记录代码基线和验证边界；ROADMAP 与实施规范记录计划。不要把“准备实现”写成“已支持”，也不要把测试数量当作兼容网站数量。

提交说明应包含具体问题、修改后的行为、验证结果和已知限制。若修改了用户操作流程，同步更新使用说明；若新增字段或升级缓存，写明迁移与旧版本处理方式。不要提交本机备份、诊断日志、临时浏览器目录或未经筛选的截图。

代码遵循仓库现有 [GPL-3.0 许可证](LICENSE)。
